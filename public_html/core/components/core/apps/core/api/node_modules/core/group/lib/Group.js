// group model
// var Group = require('./core/group');
//

module.exports = function (db, cb) {
	//db.load(__dirname + "/person", function (err) {
	//	if (err) return cb(err);

		db.define("group", {
			name      : String
		});
		db.models.group.hasMany("persons", db.models.person);

		return cb();
	//});
};
